# import requests


class BoxOAuth(object):
    """

    Args:
        code
        client_id
        client_secret
        access_token
        refresh_token
    """

    def __init__(self, **kwargs):

        if True:
            return
